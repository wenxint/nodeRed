// cs protocol
syntax = "proto3";

package ecodepb;
option go_package = "git.code.oa.com/red/ms-go/pkg/proto/ecodepb";

// Error 中保存的是除grpc status.Status中包含的grpcCode,msg等信息外的其它业务所需要的信息
message APIError {
  int64 err_code                = 1;  // 错误码, 不同服务分段使用，全局唯一，客户端根据它查表
  string tips_message           = 2;  // 错误提示，如果非空，客户端直接展示这里的描述。它可能是模版(format格式 hello {name}）
  map<string, string> tips_args = 3;  // 附加额外的一些信息，如果tips_message是模版，这是它需要的一些参数. map[name]=red
}
